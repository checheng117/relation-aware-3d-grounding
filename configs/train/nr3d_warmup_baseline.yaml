# Short real-data warmup on NR3D HF manifests (see prepare_data --mode build-nr3d-hf).
model: attribute_only
dataset_config: configs/dataset/referit3d.yaml
mode: real
debug_processed_subdir: debug
baseline_line: attribute_only
train_manifest: null
val_manifest: null
epochs: 3
batch_size: 16
lr: 0.0001
weight_decay: 0.01
seed: 42
num_workers: 2
checkpoint_dir: outputs/checkpoints_nr3d_warmup
metrics_file: outputs/metrics/train_baseline_nr3d_warmup_metrics.jsonl
run_name: baseline
device: cuda
debug_max_batches: null
