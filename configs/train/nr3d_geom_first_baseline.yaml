# Geometry-backed NR3D (prepare_data --mode build-nr3d-geom); first pass on 3090.
model: attribute_only
dataset_config: configs/dataset/referit3d.yaml
mode: real
debug_processed_subdir: debug
train_manifest: null
val_manifest: null
epochs: 6
batch_size: 24
lr: 0.0001
weight_decay: 0.01
seed: 42
num_workers: 2
checkpoint_dir: outputs/checkpoints_nr3d_geom_first
metrics_file: outputs/metrics/train_baseline_nr3d_geom_first_metrics.jsonl
baseline_line: attribute_only
run_name: baseline
device: cuda
debug_max_batches: null
