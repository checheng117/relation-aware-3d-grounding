# Copy the ``loss`` block into any train YAML to enable same-class hard-negative hinge (see ``losses.py``).
# Example:
#
# loss:
#   hard_negative:
#     enabled: true
#     margin: 0.25
#     lambda_hinge: 0.5

loss:
  hard_negative:
    enabled: false
    margin: 0.25
    lambda_hinge: 0.5
