# Baseline training config for scene-disjoint splits.
# Uses PointNet++ encoder and trustworthy scene-disjoint evaluation.
model: attribute_only
dataset_config: configs/dataset/referit3d_scene_disjoint.yaml
mode: real
debug_processed_subdir: debug
train_manifest: null
val_manifest: null
epochs: 6
batch_size: 24
lr: 0.0001
weight_decay: 0.01
seed: 42
num_workers: 2
checkpoint_dir: outputs/checkpoints_scene_disjoint_baseline
metrics_file: outputs/metrics/train_scene_disjoint_baseline_metrics.jsonl
baseline_line: attribute_only
run_name: scene_disjoint_baseline
device: cuda
debug_max_batches: null
note: "Training on scene-disjoint splits for trustworthy evaluation."