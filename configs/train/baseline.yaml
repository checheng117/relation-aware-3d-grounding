# Attribute-only (baseline_line: attribute_only) or raw-text relation (baseline_line: raw_text_relation)
model: attribute_only
dataset_config: configs/dataset/referit3d.yaml
mode: real
# mode: synthetic | real | debug  (debug uses processed_dir/<debug_processed_subdir>/ manifests)
debug_processed_subdir: debug
baseline_line: attribute_only
train_manifest: null
val_manifest: null
epochs: 5
batch_size: 8
lr: 0.0001
weight_decay: 0.01
seed: 42
num_workers: 0
checkpoint_dir: outputs/checkpoints
metrics_file: outputs/metrics/train_baseline_metrics.jsonl
run_name: baseline
device: cuda
debug_max_batches: null
