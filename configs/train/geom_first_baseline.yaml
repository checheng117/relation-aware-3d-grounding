# Geometry-backed first pass (requires real aggregations + CSV or build-nr3d-geom).
model: attribute_only
dataset_config: configs/dataset/referit3d_geom_bounded.yaml
mode: real
debug_processed_subdir: debug
baseline_line: attribute_only
train_manifest: null
val_manifest: null
epochs: 8
batch_size: 16
lr: 0.0001
weight_decay: 0.01
seed: 42
num_workers: 4
checkpoint_dir: outputs/checkpoints_geom_first
metrics_file: outputs/metrics/train_baseline_geom_first_metrics.jsonl
run_name: baseline
device: cuda
debug_max_batches: null
